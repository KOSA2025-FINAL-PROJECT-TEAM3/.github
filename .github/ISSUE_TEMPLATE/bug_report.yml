name: 🐛 버그 리포트
description: 버그나 오류를 발견하셨나요? 알려주세요!
title: "[BUG] "
labels: ["bug", "needs-triage"]
assignees: []

body:
  - type: markdown
    attributes:
      value: |
        ## 🐛 버그 리포트
        버그를 발견해주셔서 감사합니다! 아래 정보를 작성해주시면 빠르게 해결하는데 도움이 됩니다.

  - type: textarea
    id: description
    attributes:
      label: 버그 설명
      description: 어떤 버그가 발생했나요? 명확하고 간결하게 설명해주세요.
      placeholder: |
        예시: 약 등록 후 새로고침하면 등록한 약이 사라집니다.
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: 재현 방법
      description: 버그를 다시 발생시키는 방법을 단계별로 작성해주세요.
      placeholder: |
        1. '약 등록' 버튼 클릭
        2. 약 정보 입력 (이름, 용량, 복용 시간)
        3. '저장' 버튼 클릭
        4. 페이지 새로고침 (F5)
        5. 등록한 약이 사라짐
      value: |
        1.
        2.
        3.
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: 예상 동작
      description: 원래 어떻게 동작해야 하나요?
      placeholder: |
        예시: 새로고침 후에도 등록한 약이 목록에 표시되어야 합니다.
    validations:
      required: true

  - type: textarea
    id: actual
    attributes:
      label: 실제 동작
      description: 실제로는 어떻게 동작하나요?
      placeholder: |
        예시: 새로고침하면 약이 사라지고 빈 목록만 표시됩니다.
    validations:
      required: true

  - type: dropdown
    id: severity
    attributes:
      label: 심각도
      description: 이 버그의 영향도는 어느 정도인가요?
      options:
        - 🔴 Critical - 서비스 사용 불가
        - 🟠 High - 주요 기능 동작 불가
        - 🟡 Medium - 일부 기능 동작 불가
        - 🟢 Low - 사소한 문제
    validations:
      required: true

  - type: dropdown
    id: component
    attributes:
      label: 관련 컴포넌트
      description: 어느 부분에서 발생한 버그인가요?
      options:
        - 🔐 Auth (인증/로그인)
        - 💊 Medication (약 관리)
        - 👨‍👩‍👧 Family (가족 네트워크)
        - 🍽️ Diet (식단 관리)
        - 🔔 Notification (알림)
        - 📊 Dashboard (대시보드)
        - 📸 OCR (약봉지 인식)
        - ⚙️ Settings (설정)
        - 🌐 API (백엔드)
        - 🗄️ Database (데이터베이스)
        - 🎨 UI/UX (화면/디자인)
        - 기타
    validations:
      required: true

  - type: textarea
    id: screenshots
    attributes:
      label: 스크린샷 / 로그
      description: |
        스크린샷이나 에러 로그가 있다면 첨부해주세요.
        이미지는 드래그 앤 드롭으로 첨부 가능합니다.
      placeholder: |
        스크린샷을 여기에 드래그하거나 붙여넣기 해주세요.

  - type: dropdown
    id: environment
    attributes:
      label: 환경
      description: 어디에서 버그가 발생했나요?
      options:
        - 🌐 웹 브라우저 (Chrome)
        - 🌐 웹 브라우저 (Safari)
        - 🌐 웹 브라우저 (Firefox)
        - 🌐 웹 브라우저 (Edge)
        - 💻 로컬 개발 환경
        - 🐳 Docker 환경
        - ☁️ 프로덕션 환경
    validations:
      required: true

  - type: input
    id: version
    attributes:
      label: 버전 정보
      description: 사용 중인 버전을 알려주세요 (브랜치명, 커밋 해시, 버전 번호 등)
      placeholder: "예: main 브랜치, v1.0.0, commit abc123"

  - type: textarea
    id: additional
    attributes:
      label: 추가 정보
      description: 그 외 도움이 될 만한 정보가 있다면 자유롭게 작성해주세요.
      placeholder: |
        - 브라우저 콘솔 에러 메시지
        - 네트워크 요청 실패 로그
        - 특정 조건에서만 발생하는지 여부
        - 임시 해결 방법 등

  - type: checkboxes
    id: checklist
    attributes:
      label: 체크리스트
      description: 제출 전 확인해주세요!
      options:
        - label: 이미 보고된 버그인지 검색해봤습니다
          required: true
        - label: 최신 버전에서도 발생하는 버그입니다
          required: false
        - label: 재현 가능한 버그입니다
          required: false
